<%= form_with(model: journalist, local: true) do |form| %>
  <% if journalist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(journalist.errors.count, "error") %> prohibited this journalist from being saved:</h2>

      <ul>
      <% journalist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :first %>
    <%= form.text_field :first %>
  </div>

  <div class="field">
    <%= form.label :last %>
    <%= form.text_field :last %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
